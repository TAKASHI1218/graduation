<div class="blog">
  <div class='blogtitle'>
<p1>T o r i y u u - notice board</p1>
  </div>

<button type="button" class="btn btn-primary rounded-0">
<%= link_to 'ご満足度でソートする', blogs_path(sort_status: "true") %></button>

<button type="button" class="btn btn-primary rounded-0">
<%= link_to 'ご来店日でソートする', blogs_path(sort_visit_date: "true") %></button>

<button type="button" class="btn btn-primary rounded-0">
<%= link_to '作成日でソートする', blogs_path(sort_created: "true") %></button>

<% if user_signed_in? %>
<button type="button" class="btn btn-danger rounded-pill">
  <%=link_to '投稿する', new_blog_path %></button>
<% end %>

<% if current_user == nil %>
<% elsif current_user.admin %>
<button type="button" class="btn btn-success rounded-pill">
  <%= link_to '従業員一覧へ', users_path %></button>
<% end %>

<button type="button" class="btn btn-secondary rounded-0">
<%= paginate @blogs %></button>

<br>

<table class="table table-hover">
  <tr class="noticehead">
    <th>お名前</th>
    <th>題名</th>
    <th>お写真</th>
    <th>作成日</th>
    <th>ご満足度</th>
    <th>ご来店日</th>
    <th colspan="3"></th>


  </tr>

<% @blogs.each do |blog| %>
  <tr>
    <td><%= blog.user.name %></td>

    <td><%= blog.title %></td>
    <td>
    <% if  blog.picture.url %>
    <button type="button" class="btn btn-link" data-toggle="modal" data-target="#exampleModalCenter-<%= blog.id %>">
    <%=  '🐓' %>
  <% elsif blog.picture.url == nil %>
    <%= 'なし'%>
  <% end %>
    </button>
  <div class="modal fade" id="exampleModalCenter-<%= blog.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle-<%= blog.id %>" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
      <div class="modal-content">
        <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal" aria-label="閉じる">
              <span aria-hidden="true">&times;</span>
            </button>
        </div>
          <div class="modal-body">
            <div class="modalpicture">
              <p>
                <% if  blog.picture.url %>
                <%= image_tag blog.picture.url ,:width => '80%', :height => '80%' %></p>
                <% elsif blog.picture.url == nil %>
                <% end %>
            </div>
          </div>
        <div class="modal-footer">
        </div>
      </div>
    </div>
  </div>
  </td>
    <td><%= blog.created_at.strftime('%Y年%m月%d日')%></td>



    <td><%= blog.status %></td>

    <td><% if blog.visit_date %>
      <%= blog.visit_date.strftime('%Y年%m月%d日') %>
        <% else %>
      <%= blog.visit_date %>
        <% end %>
    </td>



    <td>
      <button type="button" class="btn btn-info rounded-pill"><%= link_to 'コメント', blog_path(blog.id) %></button>
    </td>

    <td>
    <% if current_user.id == blog.user_id || current_user.admin %>
      <button type="button" class="btn btn-warning rounded-pill">
      <%= link_to '編集', edit_blog_path(blog.id) %>
      </button>
    <% elsif current_user == nil || current_user.id =! blog.user_id %>
      <button type="button" class="btn btn-warning rounded-pill">
      <%= "-" %>
      </button>
    <% end %>
    </td>


    <td>
    <% if current_user.id == blog.user_id || current_user.admin %>
      <button type="button" class="btn btn-secondary rounded-pill">
      <%= link_to '削除', blog_path(blog.id), method: :delete, data: { confirm: '本当に削除していいですか？' } %>
      </button>
    <% elsif current_user == nil || current_user.id =! blog.user_id %>
      <button type="button" class="btn btn-secondary rounded-pill">
      <%= "-" %>
      </button>
    <% end %>
    </td>




  </tr>
<% end %>
</table>
</div>
